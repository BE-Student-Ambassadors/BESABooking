# Optional: leave unset in production to use this deployment's /api routes.
VITE_API_BASE_URL=

# Used only by `npm run dev` to proxy /api requests to the local backend.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
